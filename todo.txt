-(isaaclab) conda install -c -y conda-forge gcc=12
-(isaaclab) export DISPLAY="" should be in .bashrc
-(isaaclab) pip install open3d
-(isaaclab) 441702c6301a5261dbd9df6f6da175de52170169 IsaacLab git checkout

(cf_tools) pip install groq
-(cf_tools) groq setup

(mission-planner) ha nem talál útvonalat, próbálja meg a következőt

-(skeleton-mapping) filtering
-(skeleton-mapping) rerun algorithm until ... kell???
(skeleton-mapping) grid vs skeleton vs ours eval
-(skeleton-mapping) replica -> hospital
-(skeleton-mapping) cut_pcd should be here
-(skeleton-mapping) n min points at collision check
(skeleton-mapping) return only the largest subgraph

(mapping-tools) different clustering methods eval

(visualizer) add node install to docker
(visualizer) npm config set registry https://registry.npmjs.org/
(visualizer) open port 8080 for streaming


(isaaclab) add light to hospital environment
(isaaclab) record hospital environment

 